<%= link_to("HOME", {:controller => "users", :action => "show", :id => @user.id}) %>
<div>
  <h1>Public Tweets</h1>
  <% @tweets.each do |tweet| %><hr>
    <div>
      <p><%= tweet.content %> </p>
      <%= tweet.user.name %>
      <%= time_ago_in_words(tweet.created_at) %>
      <%= link_to "Like", tweet, method: '' %>
      <%= link_to "Retweet", tweet, method: '' %>
      <%= link_to "Comment", tweet, method: '' %>
    </div>
  <% end %>
</div>